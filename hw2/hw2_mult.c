#include<stdio.h>
#include<stdlib.h>

unsigned mult(unsigned a , unsigned b, unsigned moodulus, unsigned degree) {


    int extra = 0;

    while(a < 0) {

        if(  ) {
            a = a - x^0;
            extra = extra + b;
        }

        a = a / x;
        b = b * x;

        if(b) {
            // p2 = p2 - x^n  + (modulus' lower terms);
        }

    }    

}


/**
 * Caller function to test mult
 */
int main(void) {

    unsigned test;

    test = mult(2, 4, 2, 8);

    printf("You got: %i\n", test);


    return EXIT_SUCCESS;
}
